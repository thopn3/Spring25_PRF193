/*
	HelloWorld.c - The C simple program	
*/
// Khai báo thư viện sử dụng trong chương trình
#include <stdio.h> // standard input and output

// Hàm main() là một entry point (điểm đầu vào - ra của một chương trình)
int main(){
	printf("Hello World!"); // Hàm thực hiện in 1 chuỗi các kí tự ra màn hình
	return 0; // Hoàn tất quá trình chạy hàm main (nếu có lỗi sẽ tự động return 1)
}